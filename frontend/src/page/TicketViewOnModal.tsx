import React from 'react';
import type { ticket } from '../../../types';

const TicketViewOnModal = ({ticket}:{ticket:ticket}) => {
  return (
    <div>
      <h1>TicketViewOnModal</h1>
    </div>
  );
};

export default TicketViewOnModal;